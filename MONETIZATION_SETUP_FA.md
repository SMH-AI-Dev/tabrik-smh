# 📋 راهنمای فعال‌سازی کسب‌درآمد — دقیقاً چه چیزی لازم داری و کجا بگذاری

> قانون طلایی: **شماره کارت، شبا و کد ملی فقط در پنل‌های رسمی** (بازار/تپسل/زرین‌پال) وارد می‌شود —
> **هرگز** داخل کد برنامه، گیت‌هاب یا خود اپ ننویس.

## ۱) کافه‌بازار — پرداخت درون‌برنامه‌ای (مهم‌ترین، ~۱ روز کاری)

| # | چه چیزی آماده کنی | کجا ثبت می‌شود |
|---|---|---|
| ۱ | حساب کاربری کافه‌بازار + ورود به **پنل توسعه‌دهندگان** | cafebazaar.ir/developers |
| ۲ | **احراز هویت:** کارت ملی + شماره موبایل به نام خودت | پنل بازار ← حساب کاربری |
| ۳ | **تسویه درآمد:** شماره **شبا** + شماره کارت بانکی **به نام خودت** | پنل بازار ← بخش مالی/تسویه |
| ۴ | اپ را ثبت کن: نام «تبریک‌ساز SMH»، آیکون، توضیح + **APK امضاشده** (از همین ریپو: Artifacts بیلد اندروید) | پنل بازار ← اپ جدید |
| ۵ | ۳ محصول درون‌برنامه‌ای بساز و **شناسه (SKU)** بردار: `vip_monthly` / `vip_yearly` / `vip_forever` + قیمت تومانی | پنل بازار ← کسب درآمد ← پرداخت درون‌برنامه‌ای |

**کجای کد می‌رود؟** `shared/premium.js` ← تابع `Premium.pay('bazaar', plan)`:
شناسه‌های SKU را آنجا بگذار و فراخوانی SDK بازار را وصل کن (محل با TODO مشخص است).

## ۲) مایکت — مشابه بازار (نصف روز)
۱. پنل توسعه‌دهنده مایکت + احراز هویت (کارت ملی + شبا برای تسویه)
۲. ثبت اپ + محصولات IAP با همان ۳ شناسه
۳. **کجای کد؟** همان `Premium.pay('myket', plan)` در `shared/premium.js`

## ۳) تپسل — تبلیغات بنری و بین‌ابینی (۱ ساعت)
۱. ثبت‌نام **ناشر** در tapsell.ir
۲. احراز هویت: **کد ملی + شبا** (تسویه درآمد تبلیغات به همین شبا واریز می‌شود)
۳. ساخت «اپلیکیشن» در پنل → گرفتن **Zone ID** جداگانه برای بنر و بین‌ابینی
۴. **کجای کد؟**
   - بنر → `shared/index.html` جای `#adSlot` + ثابت Zone در `shared/premium.js`
   - بین‌ابینی → تابع `Premium.beforeExport()` (الان نسخه خانگی دارد؛ فراخوانی واقعی تپسل را جایگزین کن)

## ۴) زرین‌پال — فروش نسخه وب/PWA (۱ ساعت + احراز هویت)
۱. ثبت‌نام در zarinpal.com + احراز هویت (کارت ملی، موبایل به نام، **شبا**)
۲. ساخت درگاه → گرفتن **مرچنت‌کد (Merchant ID)**
۳. **کجای کد؟** دکمه زرین‌پال در مودال پرمیوم (`data-pay="zarinpal"`)
⚠️ **نکته امنیتی:** تأیید پرداخت (Verify) باید **سمت سرور** انجام شود، نه داخل کد اپ.
شروع ساده بدون سرور: «لینک پرداخت زرین‌پال» بده و بعد از پیامک واریز، **کد فعال‌سازی** را خودت برای خریدار بفرست (سیستم کد آماده است: `Premium.activate`).

## ۵) امضای اندروید — keystore (برای انتشار در بازار/مایکت)
```bash
keytool -genkey -v -keystore smh.keystore -alias smh -keyalg RSA -keysize 2048 -validity 10000
```
- فایل `smh.keystore` + سه پسورد را **فقط** در GitHub Secrets بگذار (نه در ریپو!):
`ANDROID_KEYSTORE_BASE64` • `KEYSTORE_PASSWORD` • `KEY_ALIAS` • `KEY_PASSWORD`
- بعدش بگو تا ورک‌فلو را به `assembleRelease` امضاشده ارتقا دهم.

## ۶) چک‌لیست نهایی (به ترتیب)
- [ ] شبا + کارت بانکی به نام خودت آماده است
- [ ] پنل بازار + مایکت + تپسل + زرین‌پال ثبت شده
- [ ] ۳ SKU ساخته شده و در `premium.js` گذاشته شده
- [ ] Zone ID تپسل در `#adSlot` و `beforeExport` گذاشته شده
- [ ] مرچنت‌کد زرین‌پال وصل شده
- [ ] keystore ساخته و در Secrets گذاشته شده
- [ ] 🚀 هر مورد که آماده شد، شناسه‌اش را بده تا داخل کد بگذارم و نسخه جدید بیلد بگیرم
